import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const VoiceComponent = () => {
    return (
        <View>
            <Text>VoiceComponent</Text>
        </View>
    )
}

export default VoiceComponent

const styles = StyleSheet.create({})